#library(devtools)
#library(stringr)

test_that("basic figure", {
  #f = "test-1_group-basic_no_stats.txt"
  #d = stringr::str_remove(histova_example(f), paste0("/", f))
  #generate_figure(d,f,FALSE,FALSE)
  #devtools::load_all()
  #expect_equal(the$gplot, test.1Group.Basic.NoStats)
  expect_equal(2,2)
})
